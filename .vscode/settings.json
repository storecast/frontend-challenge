// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "src/**/*.js": true,
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*?.js": {
            "when": "$(basename).tsx"
        },
        "**/*.js.map": true
    }
}